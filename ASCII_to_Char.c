// This program takes an ASCII value as input from the user and prints the corresponding character.
#include <stdio.h>

int main() {
int a;    
printf(" Enter valid ASCII value ");
scanf("%d",&a);
printf(" The Ascii value is %d and Ascii similar character is %c",a,a);

    return 0;
}