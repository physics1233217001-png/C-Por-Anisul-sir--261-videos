#include <stdio.h>

int main() {
float leanth,width,area;
printf(" Enter leanth: ");
scanf("%f",&leanth);
printf(" Enter Width: ");
scanf("%f",&width);
area = leanth*width;
printf("Area is%.2f",area);

    return 0;
}