// This program takes a character input from the user and prints its corresponding ASCII value.
#include <stdio.h>

int main() {
char c ;
printf(" Enter char to get equal Ascii Value:");
scanf("%c" , &c);
printf("The equal Ascii Value is %d",c);
    return 0;
    // you can also use the following format specifiers to read and print different types of variables:
    // %f for float, %lf for double, %c for char, %s for string, %d for integer, %u for unsigned integer, %x for hexadecimal, %
}